import React from 'react';

export interface MessageLocationProps {
	lat?: number;
	lng?: number;
	name?: string;
	address?: string;
}

const MessageLocation = (/* props: MessageLocationProps */) => {
	return <div></div>;
};

MessageLocation.propTypes = {};

export default MessageLocation;
